export enum AssessmentType {
  FORMATIVE = 'formative',
  SUMMATIVE = 'summative',
  DIAGNOSTIC = 'diagnostic',
}

export enum AssessmentStatus {
  PENDING = 'pending',
  COMPLETED = 'completed',
  REVIEWED = 'reviewed',
}
