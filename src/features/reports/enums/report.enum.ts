export enum ReportType {
  ACADEMIC = 'academic',
  ATTENDANCE = 'attendance',
  PAYMENTS = 'payments',
  BEHAVIOR = 'behavior',
  ENROLLMENT = 'enrollment',
  CUSTOM = 'custom',
  OTHER = 'other',
}

export enum ReportFormat {
  PDF = 'pdf',
  XLSX = 'xlsx',
  CSV = 'csv',
}
