export enum ForumType {
  PUBLIC = 'public',
  PRIVATE = 'private',
}

export enum ForumModerationLevel {
  NONE = 'none',
  LOW = 'low',
  HIGH = 'high',
}

export enum ForumStatus {
  ACTIVE = 'active',
  INACTIVE = 'inactive',
}
