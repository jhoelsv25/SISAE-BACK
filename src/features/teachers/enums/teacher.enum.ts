export enum ContractType {
  FULL_TIME = 'full_time',
  PART_TIME = 'part_time',
  TEMPORARY = 'temporary',
  PERMANENT = 'permanent',
}

export enum LaborRegime {
  PUBLIC = 'public',
  PRIVATE = 'private',
}

export enum WorkloadType {
  HOURS_20 = '20_hours',
  HOURS_30 = '30_hours',
  HOURS_40 = '40_hours',
}

export enum EmployementStatus {
  ACTIVE = 'active',
  INACTIVE = 'inactive',
  ON_LEAVE = 'on_leave',
}
