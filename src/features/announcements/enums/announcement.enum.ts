export enum RecipientType {
  STUDENTS = 'students',
  TEACHERS = 'teachers',
  ALL = 'all',
}

export enum AnnouncementStatus {
  DRAFT = 'draft',
  PUBLISHED = 'published',
  EXPIRED = 'expired',
}
